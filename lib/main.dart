import 'package:flutter/material.dart';
import 'package:provider/provider.dart';
import 'models/models.dart';
import 'providers/app_providers.dart';

const green=Color(0xFF00B14F), yellow=Color(0xFFF4D36B), ink=Color(0xFF17321E), soft=Color(0xFFF5F8F5);
void main()=>runApp(ChangeNotifierProvider(create:(_)=>AppState(),child:const BazarApp()));
class BazarApp extends StatelessWidget { const BazarApp({super.key}); @override Widget build(BuildContext context)=>MaterialApp(debugShowCheckedModeBanner:false,title:'Bazar USU',theme:ThemeData(useMaterial3:true,scaffoldBackgroundColor:soft,colorScheme:ColorScheme.fromSeed(seedColor:green),fontFamily:'Arial'),home:const Root()); }
class Root extends StatelessWidget { const Root({super.key}); @override Widget build(BuildContext context){ final s=context.watch<AppState>(); return s.mode==UserRole.seller?const SellerShell():const BuyerShell(); } }
class BuyerShell extends StatelessWidget { const BuyerShell({super.key}); @override Widget build(BuildContext context){final s=context.watch<AppState>(); final pages=[const HomePage(),const SearchPage(),const CartPage(),const ProfilePage()]; return Scaffold(body:SafeArea(child:pages[s.tab]),bottomNavigationBar:NavigationBar(selectedIndex:s.tab,onDestinationSelected:s.setTab,destinations:const [NavigationDestination(icon:Icon(Icons.home_outlined),selectedIcon:Icon(Icons.home),label:'Beranda'),NavigationDestination(icon:Icon(Icons.search),label:'Pencarian'),NavigationDestination(icon:Icon(Icons.shopping_bag_outlined),label:'Keranjang'),NavigationDestination(icon:Icon(Icons.person_outline),label:'Profil')]));}}
class HomePage extends StatelessWidget { const HomePage({super.key}); @override Widget build(BuildContext context){final s=context.watch<AppState>(); return ListView(padding:const EdgeInsets.fromLTRB(20,18,20,24),children:[const BrandHeader(),const SizedBox(height:18),InkWell(onTap:()=>showModalBottomSheet(context:context,builder:(_)=>AddressSheet()),child:const Card(child:ListTile(leading:Icon(Icons.location_on,color:green),title:Text('Antar ke',style:TextStyle(fontSize:12,color:Colors.grey)),subtitle:Text('Jl. Sudirman No. 12, Jakarta',style:TextStyle(fontWeight:FontWeight.bold)),trailing:Icon(Icons.keyboard_arrow_down))),const SizedBox(height:18),Container(padding:const EdgeInsets.all(18),decoration:BoxDecoration(color:green,borderRadius:BorderRadius.circular(24)),child:const Column(crossAxisAlignment:CrossAxisAlignment.start,children:[Text('Bazar USU AI',style:TextStyle(color:yellow,fontWeight:FontWeight.bold)),SizedBox(height:6),Text('Masih bingung mau makan apa?',style:TextStyle(color:Colors.white,fontSize:21,fontWeight:FontWeight.w800)),SizedBox(height:6),Text('Coba tanya sama AI kita yuuu!',style:TextStyle(color:Colors.white70)),]),),const SizedBox(height:20),const Text('Kategori',style:TextStyle(fontSize:18,fontWeight:FontWeight.w800,color:ink)),const SizedBox(height:10),Wrap(spacing:8,runSpacing:8,children:['Semua','Makanan Berat','Camilan','Minuman Segar','Dessert','Jajanan Lokal'].map((x)=>Chip(label:Text(x))).toList()),const SizedBox(height:18),const Text('Menu Pilihan Untukmu',style:TextStyle(fontSize:18,fontWeight:FontWeight.w800,color:ink)),const SizedBox(height:10),...s.products.take(6).map((p)=>ProductTile(product:p))]);}}
class BrandHeader extends StatelessWidget { const BrandHeader({super.key}); @override Widget build(BuildContext context)=>Row(children:[Container(width:44,height:44,decoration:BoxDecoration(color:yellow,borderRadius:BorderRadius.circular(14)),child:const Icon(Icons.star,color:green)),const SizedBox(width:10),const Column(crossAxisAlignment:CrossAxisAlignment.start,children:[Text('Bazar USU',style:TextStyle(fontSize:22,fontWeight:FontWeight.w900,color:green)),Text('Jajanan & Kebutuhan Kampus',style:TextStyle(fontSize:11,color:Colors.grey))]),const Spacer(),IconButton(onPressed:(){},icon:const Icon(Icons.notifications_none)),]);}
class ProductTile extends StatelessWidget { final ProductModel product; const ProductTile({super.key,required this.product}); @override Widget build(BuildContext context)=>Card(margin:const EdgeInsets.only(bottom:12),child:ListTile(contentPadding:const EdgeInsets.all(10),leading:ClipRRect(borderRadius:BorderRadius.circular(12),child:Image.asset(product.image,width:62,height:62,fit:BoxFit.cover)),title:Text(product.name,style:const TextStyle(fontWeight:FontWeight.bold)),subtitle:Text('${product.restaurant}\n★ ${product.rating}  •  20 min'),isThreeLine:true,trailing:Column(mainAxisAlignment:MainAxisAlignment.center,children:[Text('Rp${product.discountPrice}',style:const TextStyle(fontWeight:FontWeight.w800,color:green)),IconButton(onPressed:()=>context.read<AppState>().add(product),icon:const Icon(Icons.add_circle,color:green))])));}
class SearchPage extends StatelessWidget { const SearchPage({super.key}); @override Widget build(BuildContext context)=>Scaffold(appBar:AppBar(title:const Text('Pencarian')),body:ListView(padding:const EdgeInsets.all(20),children:[TextField(decoration:InputDecoration(prefixIcon:const Icon(Icons.search),hintText:'Cari makanan atau restoran',filled:true,fillColor:Colors.white,border:OutlineInputBorder(borderRadius:BorderRadius.circular(18),borderSide:BorderSide.none))),const SizedBox(height:20),...context.watch<AppState>().products.map((p)=>ProductTile(product:p))]));}
class CartPage extends StatelessWidget { const CartPage({super.key}); @override Widget build(BuildContext context){final s=context.watch<AppState>(); return Scaffold(appBar:AppBar(title:const Text('Keranjang')),body:s.cart.isEmpty?const Center(child:Text('Keranjangmu masih kosong')):ListView(padding:const EdgeInsets.all(20),children:[...s.cart.map((x)=>ListTile(title:Text(x.product.name),subtitle:Text('Jumlah ${x.quantity}'),trailing:Text('Rp${x.total}'))),const SizedBox(height:20),FilledButton(onPressed:(){},child:const Text('Lanjut ke Checkout'))]);}}
class ProfilePage extends StatelessWidget { const ProfilePage({super.key}); @override Widget build(BuildContext context){final s=context.watch<AppState>(); return ListView(padding:const EdgeInsets.all(20),children:[const BrandHeader(),const SizedBox(height:24),CircleAvatar(radius:42,backgroundColor:yellow,child:Text(s.user.name.substring(0,1),style:const TextStyle(fontSize:30,color:green,fontWeight:FontWeight.bold))),const SizedBox(height:12),Center(child:Text(s.user.name,style:const TextStyle(fontSize:20,fontWeight:FontWeight.w800))),Center(child:Text(s.user.email,style:const TextStyle(color:Colors.grey))),const SizedBox(height:24),...['Pesanan Saya','Restoran Favorit','Metode Pembayaran','Alamat Tersimpan','Pengaturan','Bantuan'].map((x)=>ListTile(leading:const Icon(Icons.chevron_right),title:Text(x),onTap:(){})),const SizedBox(height:8),FilledButton.icon(onPressed:()=>s.mode==UserRole.seller?s.switchMode():showDialog(context:context,builder:(_)=>AlertDialog(title:const Text('Beralih ke Akun Penjual?'),content:const Text('Kelola produk, pesanan, dan toko kamu melalui Dashboard Penjual.'),actions:[TextButton(onPressed:()=>Navigator.pop(context),child:const Text('Batal')),FilledButton(onPressed:(){s.becomeSeller();Navigator.pop(context);},child:const Text('Beralih'))])),icon:const Icon(Icons.storefront),label:Text(s.mode==UserRole.seller?'Kembali ke Akun Pembeli':'Beralih ke Akun Penjual')),TextButton(onPressed:(){},child:const Text('Keluar')),const Center(child:Text('BazarUSU v1.0 • Dibuat dengan sepenuh hati',style:TextStyle(color:Colors.grey,fontSize:12))) ]);}}
class SellerShell extends StatelessWidget { const SellerShell({super.key}); @override Widget build(BuildContext context){final s=context.watch<AppState>(); return Scaffold(appBar:AppBar(title:const Text('Dashboard Penjual'),actions:[IconButton(onPressed:s.switchMode,icon:const Icon(Icons.swap_horiz))]),body:ListView(padding:const EdgeInsets.all(20),children:[Text('Halo, ${s.user.name}',style:const TextStyle(fontSize:24,fontWeight:FontWeight.w900,color:ink)),const Text('Kelola toko dan pesananmu hari ini.',style:TextStyle(color:Colors.grey)),const SizedBox(height:20),Row(children:[StatCard('Pesanan Baru','12'),StatCard('Diproses','8')]),Row(children:[StatCard('Produk Aktif','24'),StatCard('Pendapatan','Rp1.250.000')]),const SizedBox(height:20),const Text('Pesanan Terbaru',style:TextStyle(fontSize:18,fontWeight:FontWeight.w800)),const SizedBox(height:10),...['#GB1024 • Ayam Geprek Mozzarella',' #GB1023 • Es Kopi Susu Gula Aren',' #GB1022 • Dimsum Sumit'].map((x)=>Card(child:ListTile(title:Text(x),subtitle:const Text('Pesanan baru • Lihat Detail'),trailing:const Icon(Icons.chevron_right)))),const SizedBox(height:20),const Text('Produk Terlaris',style:TextStyle(fontSize:18,fontWeight:FontWeight.w800)),...s.products.take(3).map((p)=>ProductTile(product:p))]),bottomNavigationBar:NavigationBar(destinations:const [NavigationDestination(icon:Icon(Icons.dashboard),label:'Dashboard'),NavigationDestination(icon:Icon(Icons.inventory_2_outlined),label:'Produk'),NavigationDestination(icon:Icon(Icons.receipt_long),label:'Pesanan'),NavigationDestination(icon:Icon(Icons.chat_bubble_outline),label:'Chat'),NavigationDestination(icon:Icon(Icons.store),label:'Toko')]);}}
class StatCard extends StatelessWidget {final String label,value;const StatCard(this.label,this.value,{super.key});@override Widget build(BuildContext context)=>Expanded(child:Card(child:Padding(padding:const EdgeInsets.all(14),child:Column(crossAxisAlignment:CrossAxisAlignment.start,children:[Text(label,style:const TextStyle(color:Colors.grey,fontSize:12)),const SizedBox(height:6),Text(value,style:const TextStyle(fontSize:18,fontWeight:FontWeight.w900,color:green))]))));}
class AddressSheet extends StatelessWidget {const AddressSheet({super.key});@override Widget build(BuildContext context)=>SafeArea(child:Padding(padding:const EdgeInsets.all(20),child:Column(mainAxisSize:MainAxisSize.min,crossAxisAlignment:CrossAxisAlignment.start,children:[const Text('Pilih Alamat Pengantaran',style:TextStyle(fontSize:20,fontWeight:FontWeight.w800)),ListTile(title:const Text('Rumah'),subtitle:const Text('Jl. Sudirman No. 12, Jakarta'),onTap:()=>Navigator.pop(context)),ListTile(title:const Text('Kampus'),subtitle:const Text('Universitas Sumatera Utara'),onTap:()=>Navigator.pop(context)),TextButton(onPressed:(){},child:const Text('Tambah Alamat Baru'))])));}
